<div class ="text-center">
<table class="table">
  <div class="d-flex align-items-end" style="height:25px;">
  <tr>
    <th>ジャンル検索</th>
  </tr>
  </div>

  <%= form_with url: genre_search_path, method: :get do |f| %>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, genre_search_path(genre_id: genre), method: :get %></td>
      </tr>
    <% end %>
  <% end %>
</table>
</div>